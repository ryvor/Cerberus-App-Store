# Cerberus App Store Repository

This repository is the official Cerberus app store.

## Layout
- `data.json` stores repository-level metadata for the app store
- `apps/` contains one folder per app
- each app folder contains a single `data.yml` manifest
- app manifests can point to external README, source, logo, and install resources as needed
